const config = {
  apiKey: 'xxxxxx',  // cionAPI.io key (1000 calls per month are free)
  currencies: 'usd,eur,btc,eth,rub',  // List of currency codes
  dbPath: '/Users/xxx/Library/Caches/moneyGuru/currency.db',  // Path to moneyGuru currency.db
};

module.exports = config;
